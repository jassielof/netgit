# NET-Git

Git-based .NET tool manager CLI. Since .NET can only install tools from NuGet, it caches the tools' repositories, builds them and installs them, using a custom alias or the tool's name.

## Dependencies

These must be installed on your system, and discoverable in your shell.

- Git
- .NET SDK

## Installation

Use astral/uv to install from git.

## Usage


